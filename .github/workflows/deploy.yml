<<<<<<< HEAD
name: Deploy to Cloud Run
=======
name: Deploy Drupal to Google Cloud Run
>>>>>>> 9e87ebca8a4627a33d99f8115e8e3880fa01d70c

on:
  push:
    branches:
      - main

jobs:
<<<<<<< HEAD
  build-and-deploy:
    name: Build and Deploy to Cloud Run
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Write service account key to file
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
        run: |
          echo "$GCP_SA_KEY" | tr -d '\r' > "${HOME}/gcp-key.json"

      - name: Authenticate with Google Cloud
        run: |
          gcloud auth activate-service-account --key-file="${HOME}/gcp-key.json"
          gcloud config set project alter-consult-464302

      - name: Configure Docker to use Artifact Registry
        run: |
          gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

      - name: Build Docker image
        run: |
          docker build -t us-central1-docker.pkg.dev/alter-consult-464302/alterconsult-repo/alter-consult-drupal:latest .

      - name: Push Docker image to Artifact Registry
        run: |
          docker push us-central1-docker.pkg.dev/alter-consult-464302/alterconsult-repo/alter-consult-drupal:latest

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy alter-consult-drupal \
            --image us-central1-docker.pkg.dev/alter-consult-464302/alterconsult-repo/alter-consult-drupal:latest \
            --platform managed \
            --region us-central1 \
            --allow-unauthenticated \
            --port 8080
=======
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, intl, pdo_mysql
          coverage: none

      - name: Set up Composer
        uses: php-actions/composer@v6
        with:
          version: latest

      - name: Install dependencies
        run: composer install --no-dev --prefer-dist --optimize-autoloader

      - name: Authenticate with Google Cloud
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}

      - name: Set GCP project
        run: gcloud config set project ${{ secrets.GCP_PROJECT }}

      - name: Build Docker image
        run: |
          docker build -t gcr.io/${{ secrets.GCP_PROJECT }}/alter_ai_agent:$GITHUB_SHA .

      - name: Push Docker image
        run: |
          docker push gcr.io/${{ secrets.GCP_PROJECT }}/alter_ai_agent:$GITHUB_SHA

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy alter-ai-agent \
            --image gcr.io/${{ secrets.GCP_PROJECT }}/alter_ai_agent:$GITHUB_SHA \
            --region ${{ secrets.GCP_REGION }} \
            --platform managed \
            --allow-unauthenticated
>>>>>>> 9e87ebca8a4627a33d99f8115e8e3880fa01d70c
