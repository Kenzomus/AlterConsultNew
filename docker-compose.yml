version: '3.8'

services:
  drupal:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - .:/var/www/html
    environment:
      - PORT=8080
    restart: unless-stopped